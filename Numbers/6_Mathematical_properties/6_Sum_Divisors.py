def sum_of(n):
    temp = n 
    total_sum = 1
    p = 2
    while p*p <= n:
        if n%p == 0
        power_sum = 1
        term = 1
        while n%p == 0:
            n //= p
            termp *= p
            power_sum += term
        total_sum *= power_sum
    p+= 1
